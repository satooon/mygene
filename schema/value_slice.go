// Generated by: gen
// TypeWriter: slice
// Directive: +gen on Value

package schema

import (
	"errors"
	"math/rand"
)

// Sort implementation is a modification of http://golang.org/pkg/sort/#Sort
// Copyright 2009 The Go Authors. All rights reserved.
// Use of this source code is governed by a BSD-style
// license that can be found at http://golang.org/LICENSE.

// ValueSlice is a slice of type Value. Use it where you would use []Value.
type ValueSlice []Value

// All verifies that all elements of ValueSlice return true for the passed func. See: http://clipperhouse.github.io/gen/#All
func (rcv ValueSlice) All(fn func(Value) bool) bool {
	for _, v := range rcv {
		if !fn(v) {
			return false
		}
	}
	return true
}

// Any verifies that one or more elements of ValueSlice return true for the passed func. See: http://clipperhouse.github.io/gen/#Any
func (rcv ValueSlice) Any(fn func(Value) bool) bool {
	for _, v := range rcv {
		if fn(v) {
			return true
		}
	}
	return false
}

// Count gives the number elements of ValueSlice that return true for the passed func. See: http://clipperhouse.github.io/gen/#Count
func (rcv ValueSlice) Count(fn func(Value) bool) (result int) {
	for _, v := range rcv {
		if fn(v) {
			result++
		}
	}
	return
}

// DistinctBy returns a new ValueSlice whose elements are unique, where equality is defined by a passed func. See: http://clipperhouse.github.io/gen/#DistinctBy
func (rcv ValueSlice) DistinctBy(equal func(Value, Value) bool) (result ValueSlice) {
Outer:
	for _, v := range rcv {
		for _, r := range result {
			if equal(v, r) {
				continue Outer
			}
		}
		result = append(result, v)
	}
	return result
}

// First returns the first element that returns true for the passed func. Returns error if no elements return true. See: http://clipperhouse.github.io/gen/#First
func (rcv ValueSlice) First(fn func(Value) bool) (result Value, err error) {
	for _, v := range rcv {
		if fn(v) {
			result = v
			return
		}
	}
	err = errors.New("no ValueSlice elements return true for passed func")
	return
}

// GroupByString groups elements into a map keyed by string. See: http://clipperhouse.github.io/gen/#GroupBy
func (rcv ValueSlice) GroupByString(fn func(Value) string) map[string]ValueSlice {
	result := make(map[string]ValueSlice)
	for _, v := range rcv {
		key := fn(v)
		result[key] = append(result[key], v)
	}
	return result
}

// Shuffle returns a shuffled copy of ValueSlice, using a version of the Fisher-Yates shuffle. See: http://clipperhouse.github.io/gen/#Shuffle
func (rcv ValueSlice) Shuffle() ValueSlice {
	numItems := len(rcv)
	result := make(ValueSlice, numItems)
	copy(result, rcv)
	for i := 0; i < numItems; i++ {
		r := i + rand.Intn(numItems-i)
		result[r], result[i] = result[i], result[r]
	}
	return result
}

// SortBy returns a new ordered ValueSlice, determined by a func defining ‘less’. See: http://clipperhouse.github.io/gen/#SortBy
func (rcv ValueSlice) SortBy(less func(Value, Value) bool) ValueSlice {
	result := make(ValueSlice, len(rcv))
	copy(result, rcv)
	// Switch to heapsort if depth of 2*ceil(lg(n+1)) is reached.
	n := len(result)
	maxDepth := 0
	for i := n; i > 0; i >>= 1 {
		maxDepth++
	}
	maxDepth *= 2
	quickSortValueSlice(result, less, 0, n, maxDepth)
	return result
}

// Where returns a new ValueSlice whose elements return true for func. See: http://clipperhouse.github.io/gen/#Where
func (rcv ValueSlice) Where(fn func(Value) bool) (result ValueSlice) {
	for _, v := range rcv {
		if fn(v) {
			result = append(result, v)
		}
	}
	return result
}

// Sort implementation based on http://golang.org/pkg/sort/#Sort, see top of this file

func swapValueSlice(rcv ValueSlice, a, b int) {
	rcv[a], rcv[b] = rcv[b], rcv[a]
}

// Insertion sort
func insertionSortValueSlice(rcv ValueSlice, less func(Value, Value) bool, a, b int) {
	for i := a + 1; i < b; i++ {
		for j := i; j > a && less(rcv[j], rcv[j-1]); j-- {
			swapValueSlice(rcv, j, j-1)
		}
	}
}

// siftDown implements the heap property on rcv[lo, hi).
// first is an offset into the array where the root of the heap lies.
func siftDownValueSlice(rcv ValueSlice, less func(Value, Value) bool, lo, hi, first int) {
	root := lo
	for {
		child := 2*root + 1
		if child >= hi {
			break
		}
		if child+1 < hi && less(rcv[first+child], rcv[first+child+1]) {
			child++
		}
		if !less(rcv[first+root], rcv[first+child]) {
			return
		}
		swapValueSlice(rcv, first+root, first+child)
		root = child
	}
}

func heapSortValueSlice(rcv ValueSlice, less func(Value, Value) bool, a, b int) {
	first := a
	lo := 0
	hi := b - a

	// Build heap with greatest element at top.
	for i := (hi - 1) / 2; i >= 0; i-- {
		siftDownValueSlice(rcv, less, i, hi, first)
	}

	// Pop elements, largest first, into end of rcv.
	for i := hi - 1; i >= 0; i-- {
		swapValueSlice(rcv, first, first+i)
		siftDownValueSlice(rcv, less, lo, i, first)
	}
}

// Quicksort, following Bentley and McIlroy,
// Engineering a Sort Function, SP&E November 1993.

// medianOfThree moves the median of the three values rcv[a], rcv[b], rcv[c] into rcv[a].
func medianOfThreeValueSlice(rcv ValueSlice, less func(Value, Value) bool, a, b, c int) {
	m0 := b
	m1 := a
	m2 := c
	// bubble sort on 3 elements
	if less(rcv[m1], rcv[m0]) {
		swapValueSlice(rcv, m1, m0)
	}
	if less(rcv[m2], rcv[m1]) {
		swapValueSlice(rcv, m2, m1)
	}
	if less(rcv[m1], rcv[m0]) {
		swapValueSlice(rcv, m1, m0)
	}
	// now rcv[m0] <= rcv[m1] <= rcv[m2]
}

func swapRangeValueSlice(rcv ValueSlice, a, b, n int) {
	for i := 0; i < n; i++ {
		swapValueSlice(rcv, a+i, b+i)
	}
}

func doPivotValueSlice(rcv ValueSlice, less func(Value, Value) bool, lo, hi int) (midlo, midhi int) {
	m := lo + (hi-lo)/2 // Written like this to avoid integer overflow.
	if hi-lo > 40 {
		// Tukey's Ninther, median of three medians of three.
		s := (hi - lo) / 8
		medianOfThreeValueSlice(rcv, less, lo, lo+s, lo+2*s)
		medianOfThreeValueSlice(rcv, less, m, m-s, m+s)
		medianOfThreeValueSlice(rcv, less, hi-1, hi-1-s, hi-1-2*s)
	}
	medianOfThreeValueSlice(rcv, less, lo, m, hi-1)

	// Invariants are:
	//	rcv[lo] = pivot (set up by ChoosePivot)
	//	rcv[lo <= i < a] = pivot
	//	rcv[a <= i < b] < pivot
	//	rcv[b <= i < c] is unexamined
	//	rcv[c <= i < d] > pivot
	//	rcv[d <= i < hi] = pivot
	//
	// Once b meets c, can swap the "= pivot" sections
	// into the middle of the slice.
	pivot := lo
	a, b, c, d := lo+1, lo+1, hi, hi
	for {
		for b < c {
			if less(rcv[b], rcv[pivot]) { // rcv[b] < pivot
				b++
			} else if !less(rcv[pivot], rcv[b]) { // rcv[b] = pivot
				swapValueSlice(rcv, a, b)
				a++
				b++
			} else {
				break
			}
		}
		for b < c {
			if less(rcv[pivot], rcv[c-1]) { // rcv[c-1] > pivot
				c--
			} else if !less(rcv[c-1], rcv[pivot]) { // rcv[c-1] = pivot
				swapValueSlice(rcv, c-1, d-1)
				c--
				d--
			} else {
				break
			}
		}
		if b >= c {
			break
		}
		// rcv[b] > pivot; rcv[c-1] < pivot
		swapValueSlice(rcv, b, c-1)
		b++
		c--
	}

	min := func(a, b int) int {
		if a < b {
			return a
		}
		return b
	}

	n := min(b-a, a-lo)
	swapRangeValueSlice(rcv, lo, b-n, n)

	n = min(hi-d, d-c)
	swapRangeValueSlice(rcv, c, hi-n, n)

	return lo + b - a, hi - (d - c)
}

func quickSortValueSlice(rcv ValueSlice, less func(Value, Value) bool, a, b, maxDepth int) {
	for b-a > 7 {
		if maxDepth == 0 {
			heapSortValueSlice(rcv, less, a, b)
			return
		}
		maxDepth--
		mlo, mhi := doPivotValueSlice(rcv, less, a, b)
		// Avoiding recursion on the larger subproblem guarantees
		// a stack depth of at most lg(b-a).
		if mlo-a < b-mhi {
			quickSortValueSlice(rcv, less, a, mlo, maxDepth)
			a = mhi // i.e., quickSortValueSlice(rcv, mhi, b)
		} else {
			quickSortValueSlice(rcv, less, mhi, b, maxDepth)
			b = mlo // i.e., quickSortValueSlice(rcv, a, mlo)
		}
	}
	if b-a > 1 {
		insertionSortValueSlice(rcv, less, a, b)
	}
}
